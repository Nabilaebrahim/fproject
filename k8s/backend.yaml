apiVersion: apps/v1
kind: Deployment
metadata:
  name: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
       
        image: 703216717940.dkr.ecr.us-east-1.amazonaws.com/gitops-webapp:backend-3ceeee17be9deb22d8b0f500262f5f83c64a7ce1
        ports:
        - containerPort: 5000
        resources: 
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        env:
        - name: DATABASE_URL
          value: "postgresql://postgres:postgres@db:5432/microservices_db"
---
apiVersion: v1
kind: Service
metadata:
  name: backend
spec:
  selector:
    app: backend
  ports:
  - port: 5000
    targetPort: 5000







    #hello